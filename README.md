# 1HomeWorkJava

## task 1
```
Декомпозиция проблемы:
1. Создание массива данных типа integer с помощью функции Math.random();
2. Параллельный вывод в терминал генерируемых данных;
3. Параллельное вычисление максимального и минимального чисел в целях экономии ресурсов системы;
4. Вывод в терминал результатов.
```
## task 2
```
Декомпозиция проблемы:
1. Создание двумерного массива;
2. Заполнение единицами ячеек, удовлетворяющих требованиям, когда значение индекса ячейки равно столбцу, либо значение индекса столбца равно размерности строки минус текущее значение индекса строки минус 1;
3. Параллельное вычисление максимального и минимального чисел в целях экономии ресурсов системы;
4. Вывод в терминал результатов.
```
## task 3
```
Декомпозиция проблемы:
1. Переменной типа integer c помощью импортированной функции _LocalDateTime_ приваивается произведенный замер времени (в часах) в реальном режиме;
2. Переменной типа  String присваивается значение времени суток, соответствующее переменной hour;
3. Осуществляется обращение к пользователю с предложением ввести имя;
4. Считанные данные с переменной timeOfDay выводятся в косоль.
```
## task 4
```
Задача решена без вспомогательных функций, использования дополнительных массивов, посредством функции _arrayOffset_, которая принимает на вход параметры типа int - массив чисел и флаг смещения массива. В функции реализована следующая логика:
1. Принятый на вход флаг приводится к числу, соответствующий размерности принятого на вход массиву;
2. Переменная _firstCompletion_ выявляет количество блоков перебора данных, в соответствии с целым остатком, приведенного к истинности флагом, от размерности массива.
3. Блок while осуществляет данную перестановку;
4. Если остаток размерности массива от приведенного флага не равно нулю, то очередной цикл осуществляет перестановку данных до того момента, пока не дойдет до индекса начальной замены;
5. Если остаток размерности приведенного флага от ☺статка длины массива к приведенному флагу не равно нулю, то, на найденную разность, осуществляет доперестановку данных.
6. Вывод в терминал результатов.
```
## task 3HomeWork
```
Декомпозиция проблемы:
1. Создание коллекции ArrayList с random-значениями;
2. Удаление значений, соответствующих условию проверки;
3. В процессе использование одного цикла:
  3.1. суммирование всех значений;
  3.2. выявление максимального значения;
  3.3. выявление минимального значения;
4. Вычисление среднего значения;
5. Вывод полученных данных в консоль.
```

кодировка UTF-8

расширение java